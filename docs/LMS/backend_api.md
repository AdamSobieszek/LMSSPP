# LMS Backend API

> [!NOTE]
> Status: placeholder. This page will document the LMS Python backend once the
> gauge refactor and experiment API are promoted.

- Describe the current [`core/lms.py`](../../src/lmsspp/core/lms.py) API.
- Describe future `LMSExperimentConfig`, `LMSInitialCondition`, and `run_lms_simulation` objects.
- Specify tensor shapes such as `[N,d]`, `[d]`, and `[d,d]`.
- Explain the separation between backend functions, widgets, and planned YAML experiments.
